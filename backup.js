var fs = require('fs');
var Request = require('request');
var qs = require('qs');


/**
 * get the listings from the openbazaar1 server
 */ 
module.exports.getListings = function getListings(cb) {
    var cb = function cb(res) {
	
    };
    request('get_listings', 'GET', '', cb) {
	
    };
}
